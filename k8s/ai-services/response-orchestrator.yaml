apiVersion: apps/v1
kind: Deployment
metadata:
  name: response-orchestrator
  namespace: ai-soc
  labels:
    app: response-orchestrator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: response-orchestrator
  template:
    metadata:
      labels:
        app: response-orchestrator
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: response-orchestrator
        image: response-orchestrator:latest
        ports:
        - containerPort: 8005
        envFrom:
        - configMapRef:
            name: ai-soc-config
        - secretRef:
            name: ai-soc-secrets
        resources:
          limits:
            memory: 512Mi
            cpu: 0.5
          requests:
            memory: 256Mi
            cpu: 0.25
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 5
          periodSeconds: 10
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: response-orchestrator
  namespace: ai-soc
spec:
  selector:
    app: response-orchestrator
  ports:
  - port: 8005
    targetPort: 8005
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: response-orchestrator
  namespace: ai-soc
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: response-orchestrator
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70